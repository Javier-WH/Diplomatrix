/*const left = e.target.getBoundingClientRect().left
  const onMouseOver = (e) => {
    console.log(left + 'px'); // con esto se obtiene la distancia desde la hoja al borde de la ventana

    //prueba de funcion
    const leftmm = pixelToMilimeter(e.target.getBoundingClientRect().left)
    console.log(leftmm + 'mm');

    //prueba de funcion
    const leftmmtopx = milimeterToPixel(leftmm)
    console.log(leftmmtopx + 'px (rec)');*/

    /*
    let _elements = JSON.parse(JSON.stringify(elements));
    let _left = _elements[1].left
    let _leftValue = _left.replace("px", "")
    let new_leftValue = Number.parseFloat(_leftValue) + 50
    _elements[1].left = new_leftValue + "px"
    setElements(_elements)
  };
  */